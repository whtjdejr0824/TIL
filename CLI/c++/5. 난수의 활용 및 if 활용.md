# 5. 난수의 활용 및 if 활용

```c++
#include <time.h>

// 난수 발생
srand(unsigned int)time(0))
    
cout << rand() << endl;
cout << rand() << endl;
cout << rand() << endl;
cout << (rand() % 101) << endl;
cout << (rand() % 10000/100.f) << endl;

return 0;
```

